# คัดลอกไฟล์นี้เป็น .env.local แล้วใส่ค่าจริงก่อน deploy
# Copy this file to .env.local and fill in real values before deploying

# URL หลักของเว็บไซต์ (ไม่มี trailing slash)
# ใช้ใน metadata, sitemap.xml, robots.txt, และ JSON-LD
NEXT_PUBLIC_SITE_URL=https://m-factory.co.th
